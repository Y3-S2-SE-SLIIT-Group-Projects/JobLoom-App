# Test Environment Configuration
# Usage: docker-compose -f docker-compose.yml -f docker-compose.test.yml up

version: '3.8'

services:
  app:
    container_name: jobloom-test
    build:
      target: development
    ports:
      - '5174:5173'
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    environment:
      - NODE_ENV=test
      - CI=true
      - VITE_API_URL=http://localhost:3001
    command: npm run test
    restart: 'no'

  # Optional: Add test database or other services
  # test-db:
  #   image: postgres:15-alpine
  #   container_name: jobloom-test-db
  #   environment:
  #     POSTGRES_USER: test
  #     POSTGRES_PASSWORD: test
  #     POSTGRES_DB: jobloom_test
  #   ports:
  #     - '5433:5432'
  #   networks:
  #     - jobloom-network
  #   tmpfs:
  #     - /var/lib/postgresql/data
